{
  "name": "port-manager",
  "version": "1.0.0",
  "description": "Desktop app showing all open ports across WSL and Windows",
  "main": "main/main.js",
  "bin": {
    "port-manager": "./bin/cli.js",
    "pm": "./bin/cli.js"
  },
  "scripts": {
    "start": "electron .",
    "test": "jest --verbose",
    "setup": "bash scripts/install.sh"
  },
  "keywords": ["ports", "wsl", "electron"],
  "license": "MIT",
  "devDependencies": {
    "electron": "^30.0.0",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "electron-store": "^8.2.0"
  }
}
